{
	"name": "my-app",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"clean": "rm -rf node_modules package-lock.json .svelte-kit && npm install"
	},
	"type": "module",
	"dependencies": {
		"@sveltejs/kit": "^2.0.0",
		"svelte": "^4.2.8"
	},
	"devDependencies": {
		"@sveltejs/adapter-netlify": "^4.1.0",
		"@sveltejs/vite-plugin-svelte": "^3.0.0",
		"@tsconfig/svelte": "^5.0.2",
		"@types/node": "^20.10.4",
		"svelte-check": "^3.6.2",
		"typescript": "^5.3.3",
		"vite": "^5.0.0"
	}
}
